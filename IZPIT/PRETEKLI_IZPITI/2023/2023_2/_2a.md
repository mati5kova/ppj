```
{ n ≥ 1 }
{ f(1)=f(1) }
m := f(1) ;
{ m=f(1) in 2=2 }
i := 2 ;
{ m=f(1) in i=2 in m=max(f(1), f(2), ..., f(i-1)) }
while i ≤ n do
    { i ≤ n in m=max(f(1), f(2), ..., f(i-1)) }
    if m < f(i) then
        { i ≤ n in m < f(i) in m=max(f(1), f(2), ..., f(i-1)) }
        { i ≤ n in f(i) ≤ f(i) in max(f(1), f(2), ..., f(i-1)) < f(i) }
        m := f(i) ;
        { i ≤ n in m=f(i) in m=max(f(1), f(2), ..., f(i-1)) }
        { i+1 ≤ n+1 in m=max(f(1), f(2), ..., f(i+1-2)) }
        i := i + 1
        { i ≤ n+1 in  m=max(f(1), f(2), ..., f(i - 2))}
    else
        { i ≤ n in m ≥ f(i) in m=max(f(1), f(2), ..., f(i-1)) }
        skip
        { i ≤ n in m ≥ f(i) in m=max(f(1), f(2), ..., f(i-1)) }
    end
    { m=max(f(1), f(2), ..., f(i-1)) }
done
{ i = n + 1 in m=max(f(1), f(2), ..., f(i-1)) }
{ m=max(f(1), f(2), ..., f(n+1-1)) }
{ m=max(f(1), f(2), ..., f(n)) }
{ m = max(f(1), f(2), . . . , f(n)) }
```

I : m=max(f(1), f(2), ..., f(i-1))
